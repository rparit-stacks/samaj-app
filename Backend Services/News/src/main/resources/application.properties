spring.application.name=NewsService
server.port=8085

# Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# Database - MySQL (separate schema for news service)
spring.datasource.url=jdbc:mysql://localhost:3306/samaj_news?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Rparit@111288
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT (must match Auth service exactly)
app.jwt.secret=samaj-auth-secret-key-min-256-bits-for-hs256-algorithm-change-in-production
